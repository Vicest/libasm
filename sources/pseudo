ft_atoi_base()
	validate_base()
	while (isspace(*str))
		str++;
	sign
	if (*str == '+')
		str++;
		go sign
	if (*str == '-')
		str++;
		go sign
	while (i = strchr(*str++, base)
		n *= base_len;
		n += i;
	ret;
