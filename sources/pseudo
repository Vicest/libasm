ft_atoi_base()
	validate_base()
		size_t bindex = -1;
		while (base[++i])
			if (base[i] == '-')
				ret;
			if (base[i] == '+')
				ret;
			if (isspace(base[i]))
				ret;
			if (next_char(base + i, i))
				ret ;
		if (bindex < 2)
			ret ;
	
